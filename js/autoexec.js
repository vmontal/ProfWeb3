"use strict";

const versione = new Date().getTime();

const adesso = () => {
    return new Date().getTime();
};


//$.getJSON("dati/versione.json?ver=" + versione, function (data) {
//    versione = data.WebStudio[0].versione;
//});
